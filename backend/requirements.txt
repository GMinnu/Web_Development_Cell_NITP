# ============================================================
# requirements.txt — Python packages to install
#
# Install all at once with:
#   pip install -r requirements.txt
# ============================================================

Flask==3.0.0                # Main web framework
Flask-SQLAlchemy==3.1.1     # Database ORM (Object Relational Mapper)
Flask-JWT-Extended==4.6.0   # JWT token creation and verification
Flask-CORS==4.0.0           # Allow Vue.js frontend to call our API
Flask-Limiter==3.5.0        # BONUS: Rate limiting
PyMySQL==1.1.0              # MySQL database driver
bcrypt==4.1.2               # Secure password hashing
bleach==6.1.0               # BONUS: XSS sanitization for HTML content
python-dotenv==1.0.0        # Load .env files for secret keys
